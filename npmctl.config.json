{
  "schemaVersion": 1,
  "site": "npmctl.com",
  "repository": "groupsum/npmctl-com",
  "orchestrator": "npmctl-pypi",
  "dns": {
    "provider": "namecheap",
    "zone": "npmctl.com",
    "records": [
      {
        "type": "A",
        "host": "@",
        "value": "SELF_HOSTED_DOCKER_IP",
        "ttl": 300
      },
      {
        "type": "CNAME",
        "host": "www",
        "value": "npmctl.com",
        "ttl": 300
      }
    ],
    "providerPackage": "npmctl-namecheap"
  },
  "deploy": {
    "target": "self-hosted-docker",
    "service": "npmctl-com",
    "composeFile": "docker-compose.yaml",
    "proxyManager": {
      "provider": "nginx-proxy-manager",
      "apiEndpoint": "https://nginx.bz23.xyz/"
    }
  },
  "commands": {
    "install": "npm ci",
    "check": "npm run check",
    "build": "npm run build",
    "docker:build": "docker compose build npmctl-com",
    "dns:plan": "npmctl plan desired-state/dns.yaml --owner npmctl-com",
    "deploy:dry-run": "node scripts/deploy.mjs --dry-run",
    "deploy": "docker compose up -d --build --force-recreate --no-deps npmctl-com",
    "npmctl:install": "python -m pip install --upgrade npmctl npmctl-namecheap",
    "npmctl:validate": "npmctl validate desired-state",
    "npmctl:schema": "npmctl schema check",
    "dns:doctor": "npmctl dns doctor --provider namecheap",
    "dns:apply": "npmctl apply desired-state/dns.yaml --owner npmctl-com",
    "proxy:validate": "npmctl validate desired-state/proxy.yaml",
    "proxy:plan": "npmctl plan desired-state/proxy.yaml --owner npmctl-com",
    "proxy:apply": "npmctl apply desired-state/proxy.yaml --owner npmctl-com"
  },
  "npmctl": {
    "package": "npmctl",
    "dnsProviderPackage": "npmctl-namecheap",
    "installCommand": "python -m pip install --upgrade npmctl npmctl-namecheap",
    "proxyManagerApiEndpoint": "https://nginx.bz23.xyz/"
  }
}
